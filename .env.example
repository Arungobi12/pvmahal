# Environment Variables for PV Mahal
# Copy this file to .env.local and update with your actual values

# Backend API URL (if you have a backend server)
VITE_API_BASE_URL=

# Google Sheets Web App URL (for automatic data collection)
# To set this up:
# 1. Create a Google Sheet
# 2. Go to Extensions > Apps Script
# 3. Create a web app that accepts POST requests
# 4. Add the deployed web app URL here
VITE_GOOGLE_SHEETS_URL=

# Example Google Apps Script code for your Google Sheet:
# function doPost(e) {
#   const sheet = SpreadsheetApp.getActiveSheet();
#   const data = JSON.parse(e.postData.contents);
#   
#   sheet.appendRow([
#     data.id,
#     data.name,
#     data.email,
#     data.phone,
#     data.eventType,
#     data.eventDate,
#     data.timeSlot,
#     data.guestCount,
#     data.specialRequests,
#     data.status,
#     data.createdAt
#   ]);
#   
#   return ContentService.createTextOutput("Success");
# }